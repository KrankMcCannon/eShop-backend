{{!-- <h1 class="text-center mb-3">{{title}}</h1>
<h4>Products</h4>
<ul class="list-group">
  {{#each products}}
  <li class="list-group-item">{{this.name}}: {{this.price}}</li>
  {{/each}}
</ul> --}}
